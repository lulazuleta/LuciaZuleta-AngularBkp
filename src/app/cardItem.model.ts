export class CardItem {
    constructor(public name : string, public price: number, public quantity: number, public type: string){
this.name = name;
this.price = price;
this.quantity = quantity;
this.type = type;

    }
}